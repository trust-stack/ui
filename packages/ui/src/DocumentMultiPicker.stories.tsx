import { Meta, StoryObj } from '@storybook/react';
import { DocumentMultiPicker } from './DocumentMultiPicker';

export default {
    component: DocumentMultiPicker,
} as Meta<typeof DocumentMultiPicker>;

type Story = StoryObj<typeof DocumentMultiPicker>;

export const Basic: Story = {};
